apiVersion: v1
kind: Service
metadata:
  name: tauto
  namespace: {{ .Release.Namespace }}
  labels:
    app: tauto
spec:
  type: ClusterIP
  selector:
    app: tauto
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
      name: http
